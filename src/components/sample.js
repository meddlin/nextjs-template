/**
 * Sample component
 * This sample component is only here to demonstrate a folder structure, and
 * running tests on components vs "pages". Feel free to follow this convention
 * or not.
 */
const Sample = () => {
    return (
        <div>
            <h1>Sample Component</h1>
        </div>
    );
};

export default Sample;